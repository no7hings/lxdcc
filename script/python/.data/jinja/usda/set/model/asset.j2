#usda 1.0
(
    defaultPrim = "{{ dcc.root }}"
    metersPerUnit = 0.01
    upAxis = "Y"
)

def Xform "{{ dcc.root }}" (
    assetInfo = {
        asset identifier = @{{ asset }}.usda@
        string name = "{{ asset }}"
        string version = "{{ version }}"
    }
    kind = "component"
    payload = @{{ usdas.payload }}@
)
{
}
