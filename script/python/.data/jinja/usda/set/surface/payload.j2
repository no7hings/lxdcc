#usda 1.0
(
    defaultPrim = "{{ dcc.root }}"
    metersPerUnit = 0.01
    upAxis = "Y"
)

def "{{ dcc.root }}" (
    prepend references = [
        @{{ geometries.hi }}@,
        @{{ geometries.shape }}@,
        @{{ geometries.hair }}@
    ]
)
{
    over "hi" (
        kind = "subcomponent"
    )
    {
        uniform token purpose = "render"
    }

    over "hair" (
        kind = "subcomponent"
    )
    {
    }

    over "shape" (
        active = false
        kind = "subcomponent"
    )
    {
    }
}
