#usda 1.0
(
    defaultPrim = "{{ root }}"
    upAxis = "{{ preset.up_axis }}"
    metersPerUnit = {{ preset.linear_unit }}
)

def Xform "master" (
    kind = "component"
{{ '%spayload = @%s@' | format(option.indent*1*' ', payload_file_path) }}
)
{
}